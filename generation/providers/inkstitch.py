"""Provedor Ink/Stitch — stub para integracao futura.

NÃO usa Inkscape headless (regra do projeto). Integracao pendente (PENDENCIAS_EXTERNAS.md B).
"""


def generate(arte_path: str, params: dict) -> str:
    """Stub — integracao futura com Ink/Stitch CLI (nao headless)."""
    raise NotImplementedError(
        "Integracao com Ink/Stitch CLI pendente. "
        "Ver PENDENCIAS_EXTERNAS.md B."
    )
